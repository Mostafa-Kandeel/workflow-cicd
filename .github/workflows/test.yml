
on:
  push:
    branches: [ "main" ]

jobs:
  build new app:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3  
      - name: print hello
        run: echo "hello world"  paths:
    
